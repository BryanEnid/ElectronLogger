import React from 'react';
import { Box } from '@mui/material';

export const Fab = () => {
  const size = 60;
  return (
    <Box sx={{ height: size, width: size, background: 'red', borderRadius: size / 2 }}>
      Fabasdasd
    </Box>
  );
};
